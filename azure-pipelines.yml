# Azure pipeline reference: https://aka.ms/yaml

trigger:
- master

pr:
- master

pool:
  vmImage: 'ubuntu-latest'

steps:
- pwsh: ./bootstrap.ps1
  displayName: Bootstrap
- pwsh: ./build.ps1
  displayName: Build
- pwsh: cd build && ctest
  displayName: Test